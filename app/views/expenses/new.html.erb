<div class ="expense-main">

  <h1>支出の登録</h1>

  <div class="expense-form">
  <%= render "devise/shared/error_messages", resource: @expense %>
    <%= form_with(model: @expense, local: true) do |f| %>
      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-text">支出の種類</label>
          <span class="indispensable">必須</span>
        </div>
        <%= f.collection_select(:expense_category_id, ExpenseCategory.all, :id, :name, {}, {class:"select-box", id:"prefecture"}) %>
      </div>

      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-text">支払方法</label>
          <span class="indispensable">必須</span>
        </div>
        <%= f.collection_select(:payment_method_id, PaymentMethod.all, :id, :name, {}, {class:"select-box", id:"prefecture"}) %>
      </div>


      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-text">金額</label>
          <span class="indispensable">必須</span>
        </div>
        <%= f.text_field :price, class:"input-default", id:"postal-code" %>
      </div>

      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-text">日時</label>
          <span class="indispensable">必須</span>
        </div>
        <%= f.date_field :date, class: 'date-select' %>
      </div>

      <div class='buy-btn'>
        <%= f.submit "登録" ,class:"buy-blue-btn", id:"button" %>
      </div>
    <% end %>
    </div>

    <br>
